# Спека модуля: {module_name}

> Копировать этот файл в `specs/{module_name}.md` и заполнять по разделам. Раздел
> "Критерии приёмки" пишется в формате Given/When/Then — он напрямую превращается в тесты
> (см. WORKFLOW.md).

## 1. Назначение

Одно-два предложения: что делает модуль и зачем он нужен.

## 2. Границы (Scope)

**Входит в модуль:**
-

**Явно НЕ входит (Out of scope):**
- (например: "экспорт в CSV — не в этой версии")

## 3. Модель данных

Таблица на каждую сущность:

| Поле | Тип | Обязательное | Ограничения / примечания |
|---|---|---|---|
| id | UUID | да | PK |
| ... | ... | ... | ... |

Явно указать:
- Схема Postgres: `{module_name}`
- Денежные поля — integer (копейки), если применимо
- Часовой пояс/даты — как хранится (date vs timestamp), в каком TZ считается "день"

## 4. API-контракт

| Метод | Путь | Запрос | Ответ | Коды |
|---|---|---|---|---|
| POST | /api/{module_name}/... | `{...}` | `{...}` | 201, 400, 422 |
| GET | /api/{module_name}/... | — | `{...}` | 200 |

Для каждого эндпоинта — что происходит при отсутствии ресурса (404), при невалидных
данных (422), при попытке доступа к чужим данным (403/404).

## 5. События

**PUBLISHES** (что модуль публикует в шину):

| Событие | Когда | Payload |
|---|---|---|
| `{module}.{action}` | ... | `{...}` |

**SUBSCRIBES_TO** (на что модуль подписан):

| Событие | Источник | Обработчик делает |
|---|---|---|
| `other_module.event` | другой модуль | ... |

## 6. Бизнес-правила и edge cases

Список правил, которые не очевидны из модели данных напрямую — например, как считается
стрик, что происходит при одновременном редактировании, что происходит на границе дня
по часовому поясу, что происходит при удалении сущности, на которую ссылаются другие.

## 7. Критерии приёмки

Формат Given/When/Then. Каждый пункт = минимум один тест.

1. **Given** ... **When** ... **Then** ...
2. **Given** ... **When** ... **Then** ...
3. ...

## 8. Нефункциональные заметки

Опционально: производительность (если объём данных большой — например, food), доступ
только к своим данным, специфичные для модуля соображения безопасности.

## 9. Открытые вопросы

Всё, что ещё не решено — явно вынесено сюда, а не оставлено подразумеваемым в других
разделах. Спека не считается утверждённой, пока этот раздел не пуст.
